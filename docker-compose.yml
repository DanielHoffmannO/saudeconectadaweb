version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data

  web:
    build:
      context: ./web
    ports:
      - "8080:80"
    depends_on:
      - api
